<!--components/Tabs/Tabs.wxml-->
<view class="tabs_container">
	<view class="tabs_title">
		<view class="tabs_title_item  {{item.isActive?'active':''}}" wx:for="{{tabs}}" wx:key="id" bindtap="itemChangeSeletced" data-flag="{{item.id}}">
			{{item.value}}
		</view>
	</view>
   <view class="tabs_content">
     <solt></solt>
    </view>
</view>